# Local dev defaults, committed on purpose (no secrets — identity lives on the
# API). Run the API alongside: cd apps/api && npx wrangler dev (port 8787).
VITE_API_URL=http://localhost:8787
VITE_SITE_URL=http://localhost:3200
